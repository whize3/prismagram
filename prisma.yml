#endpoint: ${env:PRISMA_ENDPOINT}endpoint: https://us1.prisma.sh/workspace-whize/prismagram/dev

datamodel: datamodel.prisma

generate:
  - generator: javascript-client
    output: ./generated/prisma-client/
